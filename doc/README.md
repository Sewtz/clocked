# Clocked — documentation index

| File | Purpose |
| --- | --- |
| `discussion.md` | Live scratchpad while shaping the app. Move settled items out of here. |
| `idea.md` | Product intent, users, scope. |
| `architecture.md` | Components, data model, runtime flow, storage, PWA wiring. |
| `decisions.md` | Decision log (ADR-style): what we chose, what we rejected, why. |
| `test-vnv-strategy.md` | Test layers and the V&V steps to run after every change. |
| `plan/README.md` | Implementation plan index. Read this first when starting implementation. |
| `plan/00-scaffolding.md` … `plan/07-vnv.md` | One file per work package (WP0…WP7), with tasks, subtasks, code skeletons, and acceptance criteria. |

These docs are the source of truth for intent and constraints. `AGENTS.md` references them and keeps the operational guidance for working in the repo.
